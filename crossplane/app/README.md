# README

```bash
# Create the package (where 'crossplane.yaml' is located)
kubectl crossplane build configuration --name app

# Push configuration to 
kubectl crossplane push configuration jsa4000/crossplane-app:v0.1.0
```
